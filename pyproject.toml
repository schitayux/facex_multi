[project]
name = "facex_multi"
authors = [{ name = "CHAPPSA", email = "soporte@chappsa.com" }]
description = "FacEx Multi — Interfaz rápida tipo POS para Sales Invoice con certificación FEL"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "facex_multi"
